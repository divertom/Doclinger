# Port configuration for Docker Compose
# Copy this file to .env and adjust ports as needed (or set environment variables)

# Main service ports (default: API 8001, UI 8502)
API_PORT=8001
UI_PORT=8502

# Dev profile ports (default: API 8002, UI 8503)
# Used when running: docker compose --profile dev up
API_PORT_DEV=8002
UI_PORT_DEV=8503

# Backend URL (for UI when running locally)
# BACKEND_URL=http://localhost:8001
